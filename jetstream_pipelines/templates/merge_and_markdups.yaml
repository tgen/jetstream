{% for sample in project.samples() -%}

- id: merge_bams_{{ loop.index }}
  cmd: sbatch_wait -c8
  stdin: |
    #!/bin/bash
    set -eu
    module load gatk/4.0.1.2

    # TODO merge bams

{% endfor %}
